<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.bit.mybatis.ProMapper">

	<!-- Product List -->
	<select id="listPro" resultType="com.bit.shop01.product.ProductVo">
		SELECT
			productNum,
			productName,
			price,
			explan,
			product_url
		FROM product
		ORDER BY productName
	</select>
	
	<!-- Product Detail -->
	<select id="detailPro" resultType="com.bit.shop01.product.ProductVo">
		SELECT
			productNum,
			productName,
			price,
			explan,
			product_url
		FROM product
		WHERE productNum = #{productNum}
	</select>

</mapper>